//Functions

//older way to define function
function multiply(a, b) {
  return a * b;
}

console.log(multiply(4, 5));

//New way to define function   || Arrow Function
const multiply = (a, b) => {
  return a * b;
};

console.log(multiply(4, 5));
